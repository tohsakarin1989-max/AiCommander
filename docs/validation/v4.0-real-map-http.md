# 真实后端离线地图组件验证

2026-09-10，真实公开地图包 → 完整Worker校验 → 登记 → 发布 → 实际FastAPI → Chrome。
使用独立临时SQLite和文件目录、专用测试账号与Cookie，未改业务系统 current。

## 最终复测结果

- 快照：`923eb887-33d9-4835-94d7-e004f4cf0e55`。
- 真实登录返回200；四组件为案件地图、选点、时空地图、辖区设施地图。
- 每个组件均等待加载提示消失，分别检查真实样式/瓦片200；选点回调成功。
- 共16次瓦片200、236次字形200；真实地名查询“大庆”成功。
- 全新浏览器上下文并清缓存，浏览器拦截公网请求，实际公网请求0、页面异常0、API 5xx为0。
- 没有API模拟响应，退出0；截图已目视确认可见公共底图。
- 图像：`output/playwright/real-map-http/{cases,picker,heat,assets}.png`。

首次自动化未等待底图ready导致选点断言失败；补充逐组件ready检查后通过。
测试服务复核发现可能继承原broker和Cookie名，已停止旧进程并整改：
导入app前切换临时目录、清空继承环境（只保留必要运行路径）、使用内存Celery、
不可用的本次临时Unix Redis路径、关闭Agent、使用专用Cookie；重启后上述最终复测通过。
第一次运行只访问登录和地图读取接口，未调用案件/任务创建接口。

## 可重复入口

1. 使用地图依赖环境运行 `AIC_DISPOSABLE_MAP_SMOKE=1 ... python scripts/serve-isolated-map-check.py`。
   自动构建本次临时地图数据库，仅监听127.0.0.1:18049；停止进程后清理临时地图和库。
2. 独立Vite通过 `VITE_API_PROXY_TARGET=http://127.0.0.1:18049`，监听127.0.0.1:13042。
3. 使用已有Playwright依赖运行 `node scripts/verify-real-offline-map.cjs`。
   可用 `AIC_PLAYWRIGHT_MODULE` 指定本机已有模块，不需要新增测试框架。

## 尚未证明的范围

浏览器使用真实业务组件的隔离入口，不是正式案件/辖区/态势页面的完整操作流程。
浏览器公网拦截不代表服务器防火墙已断公网，尽管地图接口本次只读取内网文件。
复杂文字排版仍未全面验收；当前能力不含机动车路由，不能把本结果视作道路通行证明。
正式业务页面、服务器断网、自动发布、实际目标机性能仍需后续集成验收；不声明v4.0 Stable。

## 后续容器与正式页面验收（2026-09-10）

上述是首轮记录，以下取代其中“服务器断网、自动发布、正式页面尚未验证”的状态，
但不取代完整业务流程和目标机性能验收。

- 新地图镜像 config `sha256:abf89861066d65b3f548cb45bb0bce6efc0d395e210e8a454346654242c9be9e`。
- 真实 Redis/Celery/Beat 完成公开包导入、验包、登记和自动发布；
  快照 `ceedad27-b860-4438-8590-dd0419af6b30` 成为隔离库唯一 current。
- API 仅加入 Docker internal 网络，无公网网关；实际公网连接返回 101（Network unreachable）。
  浏览器通过固定上游的本机入口代理访问 API，未改实际业务库。
- 四个正式页面 `/cases/map`、`/jurisdiction`、`/cases/spacetime`、`/dashboard`
  均加载真实样式和瓦片；原组件四项验证同时通过。该轮 38 次瓦片、472 次字形成功，
  浏览器公网请求 0、页面异常 0，无 API mock。
- 修复辖区短地图容器的空白：适配器 GL 级别比 Leaflet 少一级，分别约束原生与显示级别。
- 大屏原来仍使用栅格加载器，现统一使用离线矢量/栅格加载能力及真实加载状态。
- 修复热力图切页后旧 RAF 访问已销毁地图；相关 46 项测试和生产构建通过。
- 截图为 `output/playwright/real-map-http/page--*.png`，已目视确认底图可见。

仍未把“打开正式页面”计作案件录入、编辑、导入和报告的完整流程通过。
道路约束、复杂文字完整排版及目标机性能仍未验证，不声明 Stable。

### 切页收口复测

- 大屏统一关闭 Leaflet 缩放动画；共享适配器接管可取消的 transition RAF，
  移除时取消并阻断晚到的 GL moveend，覆盖 resize 分支而不只覆盖缩放。
- 3 项新增生命周期测试通过；独立复核相关 22 项测试通过，类型检查、生产构建通过。
- 实际 Chrome 重跑四组件、四正式页面；大屏放大、全屏后通过 SPA 切至案件页，
  地图卸载后页面异常 0；仍为 38 瓦片/472 字形成功、公网请求 0，脚本退出0。
- 该修补仅针对固定适配器 0.1.4；移除后重试创建新 layer，不支持复用已销毁 layer 实例。
- 此批地图接入验证收口。下一工作包为隔离展示入口，不再次重复构建该公开地图包。
