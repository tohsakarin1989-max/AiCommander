# 组合字体完整地图候选

2026-09-10；v4.0 开发实物验证，未发布 current，版本号仍为 3.6.0-stable。

## 生成与来源固定

沿用原生成任务至正常结束，没有因观察超时而重启。生成器使用固定 WASM 和三份固定 Noto 字体，新输出位于 `backups/map-foundation/v4-source/20260908/glyphs-composite-v1/`。

- 257 个分段，32,507,193 字节；
- 42,853 个有效字形 ID，42,839 个通过位图阈值检查的可绘制字形；
- 包含 U+1F3E1 和 U+1F3E5；
- 完整字形清单 SHA-256：`3c8637c94a68f9f47fdd2c373435e3506492230c19e4a3914502a3a2198ae308`。

所有分段实际读取并核对大小、摘要、嵌入字体名称和范围，不以清单声明代替文件解码。

## 完整新候选

构建入口增加显式 `--font-profile cjk-mongolian-emoji-v1`；默认仍为旧 CJK 模式，旧包身份和署名保留。组合模式将固定字形清单、前端样式和三份完整 OFL 许可绑定到同一传输清单。

新目录：`backups/map-foundation/v4-source/20260908/complete-candidate-v2/`。

- 逻辑包标识：`daqing-qiqihar-20260908-candidate-v3`；
- 270 项资产，288 个分片，总资产 356,967,415 字节；
- 清单 SHA-256：`d4ab1cf9b73f4352b437f81939aed2a86f2afd313fc12185690bb3af772ede48`；
- 重组目录：`map-assembly-muyp0orr`；
- 构建、传输读回、重组和前端官方样式语法校验通过；
- CJK、Mongolian、Emoji 的完整许可分别保留，不只留链接。

原 `complete-candidate-v1`、原字体和当前地图未覆盖。

## 实际验收结果

在独立地图运行环境执行整包校验，结果 `render_content_validated`：

- 全部 1,128,893 个瓦片解码，627,261 个非空瓦片，16 个图层；
- 257 个字形分段与 14 层样式的组合字体身份一致；
- 21,666 个地点、31,363 个名称、426,004 个搜索片段通过索引内容及来源绑定校验；
- 双分辨率图标资产解码一致；当前样式不使用图标。

另用修复后的全图标签审计重新扫描同一固定矢量资产：394,909 次标签出现、2,608 个非空白码位，与已逐文件核验的组合字形求差，`missing_drawable=[]`。

这证明指定标签字段的字形码位覆盖，不证明复杂文字连写、碰撞避让或浏览器最终布局。`shaping_verified=false`，完整包仍为 `publish_ready=false`，未手动篡改验收标志。

## 测试与发布边界

按 tdd-workflow 先补组合制包失败测试再实现；保留旧模式真实样式导出测试。独立只读审查通过。

- 构建脚本 133 项通过（5.17 秒）；本轮制包模块行覆盖率 91%。
- 组合制包专项 8 项通过，覆盖新旧样式、许可数量和拒绝覆盖已有输出。
- 链条回归 4 项通过；前端类型检查、生产构建及差异检查通过，保留既有大包警告。
- 尚需浏览器实际文字显示、地域/道路约束验收、后台正式登记、原子发布/回滚、断公网冷启动，以及其余 v4.0 智能查询/展示门禁。

本轮没有操作正式业务数据库、Git 提交、标签或 GitHub Release。
