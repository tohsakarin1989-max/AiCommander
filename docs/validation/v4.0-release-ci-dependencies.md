# 发布CI地图校验依赖补齐

2026-09-10，发布候选核对发现环境差异：后端全量测试包含真实官方地图样式校验，
但GitHub后端job只安装Python依赖。本机已有frontend/node_modules会掩盖该缺口。

修复 `.github/workflows/release-quality.yml`：后端测试前使用Node24，按前端锁文件执行
`npm ci --omit=dev --ignore-scripts --no-audit --no-fund`，与地图Worker所需运行库一致。
不将Node加入核心API镜像，不删除或模拟真实样式校验测试。

验证：

- 新增CI契约测试先得到1失败、2通过，失败位置为缺少Node安装前置步骤。
- 修复后版本契约及地图样式专项17项通过，0.84秒。
- 在全新临时目录复制package.json、锁文件及校验入口，离线安装146个包成功。
- 该新目录的真实样式校验返回`style_syntax_validated`，校验器版本26.4.2，退出0。
- 差异检查通过。

上述记录证明配置前置条件、专项回归和本机干净依赖安装；生产API无需Node，地图导入Worker独立携带它。

## 2026-09-11 远端首轮结果与修复

PR #17 的运行34490693010已结束：前端和部署配置通过；后端996通过、1跳过、1失败。
官方样式校验及依赖安装均通过。唯一失败是部署测试把Compose默认版本写死为3.6.0-stable，
未跟随VERSION升级到4.0.0-stable。现改为核对VERSION与Compose默认值一致，保留一致性断言。
版本、部署与样式专项21项通过（2.67秒）。

同时为1MiB超限输入和深度嵌套JSON设置简短测试ID；测试输入和断言不变。
这是日志可读性修正，不能据此断言此前远端耗时全部由日志引起。
任务最终日志报告测试耗时104.25秒；任务显示的完成时间与早期输出存在较大间隔。
本修复尚需新的远端检查通过，不沿用失败任务作为Stable通过证据。

修复后的本机全量回归：997通过、1项专用PostGIS连接测试跳过，40.86秒。
与远端首次测试总数同为998项；没有删掉失败测试或放宽生产断言。
