# Valhalla 真实构图与限制结构检查

2026-09-10；v4.0 前置验证，不是 v4.2 道路研判交付，也不是 Stable 发布。

## 实物构建

固定 pyvalhalla 3.8.3 包内原生构图程序，对 `two-city-roads-v1/roads.osm.pbf` 实际构图，退出码0。
输入摘要 `bfa43dbb42c191d4e8e0a00e390bdcca327d7bc3c4502d879c0f8a719b887b2a`。
成果位于忽略目录 `backups/map-foundation/v4-source/20260908/valhalla-graph-v1/`，没有接入业务接口或替换 current。

- 302个最终 gph 文件，合计22,433,848字节。
- 按相对路径排序，将每文件 `路径\t字节数\tSHA256\n` 累积计算的清单摘要：`3e0ff861f57a1d1ee0d7248be4a1df6e39657861e77bb0d7df669d2ed2d98ae4`。
- 构建配置摘要：`4c30ff0592cb51f20b5f0c588a6d0e062ec42fc71ce2f8863b5de01c47f68707`。
- 引擎解析43,640条道路、7条简单转向限制；这些数量与原始要素数量不是同一口径。
- 图归一化增加56个尽端道路环形结构；未改写原始PBF，不将派生图宣称为原始几何逐项复制。
- 原生验证提示中间层4项潜在重复，尚待进一步定位；不能用退出码0消除此提示。

这是诊断配置：未配置行政、时区、高程、实时交通数据库，关闭国家属性推断，不代表历史时段和地区通行规则可用于生产。

## 实际路径冒烟查询

使用本地 Actor、auto参考车型、单位公里、无日期参数；两组公共城区测试坐标不是案件、井场或真实行驶轨迹。

| 测试 | 输入起终点（经度，纬度） | 引擎状态 | 返回道路距离 |
|---|---|---|---|
| 大庆 | (125.1038,46.5893) → (125.1210,46.6020) | 0 | 3.143 km |
| 齐齐哈尔 | (123.9182,47.3543) → (123.9480,47.3470) | 0 | 3.421 km |

引擎会吸附到道路；这些是可执行性检查，未人工核验每一段实际通行条件，不作为禁转、道路正确率或时间准确率验收。

## 新发现：引用齐全但角色缺失

新版 `scripts/audit-public-osm.py` 将引用检查与成员结构检查分开。实际 `reference-and-structure-audit-v2.json`：

- 引用门槛 passed：缺道路节点0，缺已列出的限制成员0。
- 结构门槛 failed：13条关系中6条缺少必需角色。
- 缺from：r8478829、r15324173。
- 缺to：r8478830、r8478831、r8478832、r12332373。
- 原生引擎仅解析7条限制，与结构检查结果吻合；不能宣称全部禁转生效。

保留异常及原始来源，不猜测补齐、不删除记录以通过验收。后续需要核实原始公共来源或为涉及路口明确隔离计算范围；任何修正应保留版本和证据。

结构审计接受no_entry多from、no_exit多to；识别方式限定和条件限定记录但明确标注unverified。结构通过仍不证明道路相连、转向语义正确或引擎支持。
CLI在结构失败或未验证时返回非零，报告仍保留；旧引用报告不覆盖。

## 验证与剩余工作

- 21项审计测试、4项链条回归通过，独立复核未发现高优先级问题。
- 前端类型检查和生产构建通过，仍有既有的大包警告。
- 本轮后端全量799项通过、1项跳过，36.76秒；跳过项不是PostgreSQL现场验证通过。
- 真实单行、禁转、立交不连通三组固定样例已通过下述断言验证；边界绕行仍未验证。
- 尚需道路限制缺项处理、行政时区配置、接口权限及受控路网条件版本；不能直接开放此候选图供业务研判。
- 全部4.x路线继续按版本验收推进，本记录不变更VERSION、GitHub标签或发布状态。

## 固定真实道路约束回归（2026-09-10追加）

`scripts/verify-public-road-constraints.py` 读取上述原始 PBF 与本地构图成果，
先核对源文件和302个图文件摘要，再读取原始标签、限制关系、节点及几何。
图文件摘要中的相对路径以 `tiles/` 开头。查询严格核对实际吸附道路编号，
通过引擎 `edge_walk` 获取返回路线所经道路，不用直线距离或渲染图片代替路径验证。

| 样例 | 原始对象 | 实际结果与断言 |
|---|---|---|
| 单行 | w250284015，oneway=yes | 顺向0.319公里且只经原道路；逆向2.244公里，经连接道路绕行 |
| 禁左转 | r21241409，from w1550481791、via n14102424710、to w1550481790 | 路线0.687公里；返回路径中不存在禁止的直接 from→to 相邻边 |
| 立交不连通 | 桥梁w250284015、地面道路w772434747 | 原始线形相交但节点不共享；路线0.751公里，经连接道路而非在几何交叉点直接转入 |
| 行政边界接续 | 绥满线w83279635 | 起点位于两市范围内、终点位于范围外；0.422公里路线连续通过同一道路，没有在行政边界截断 |

实际本地引擎执行退出码0；所有起终点吸附距离均为0米。未访问外部路由服务，
坐标仅为公共道路验证点，不是案件或生产设施数据。

复跑方式（隔离构图环境安装 pyvalhalla 3.8.3、osmium、shapely）：

```sh
python scripts/verify-public-road-constraints.py \
  --source backups/map-foundation/v4-source/20260908/two-city-roads-v1/roads.osm.pbf \
  --config backups/map-foundation/v4-source/20260908/valhalla-graph-v1/build-config.json \
  --boundaries backups/map-foundation/v4-source/20260908/two-city-region-v3/city-boundaries.geojson
```

输出包括输入摘要、全部路径道路编号、查询点和吸附记录；源数据或图版本不符直接失败。
`fixture_status=passed` 仅表示这些固定样例，`network_certified=false` 始终明确保留。
边界文件摘要固定为 `ef9f162093c0368e55b3933597b3c6132ce1766c498113057b76332b2badd82d`。
边界接续不等于“两端均在辖区内但路径绕行辖区外”的验证；后者、六条异常限制关系及
业务路由验收仍未通过，不开启业务路由能力。脚本拒绝非空 `tile_extract`、`tile_url`、
`traffic_extract`，避免实际引擎改用未校验图源；所有四组样例最终复跑退出码0。

## 六条缺项的来源追溯（2026-09-10追加）

按原始关系ID对黑龙江、吉林、内蒙古下载PBF、合并PBF及道路提取PBF做只读成员比对。
六条关系全部存在于黑龙江原始下载文件，标签、版本及成员列表与合并、提取结果完全一致；
吉林和内蒙古文件未包含这些关系。因此当前缺项不是本项目合并或提取丢失成员，
重新构图不能补齐缺失语义。此结论仅针对已下载快照，不冒充OSM当前在线记录已核验。

| 关系 | 原始版本 | 已有via节点 | 缺失角色 |
|---|---:|---|---|
| r8478829 | 2 | n2330188538 | from |
| r8478830 | 2 | n2330188538 | to |
| r8478831 | 2 | n5787228435 | to |
| r8478832 | 2 | n5787228432 | to |
| r12332373 | 2 | n8423985748 | to |
| r15324173 | 1 | n10247932574 | from |

继续保留原始对象和审计失败记录；不依据道路形状猜测缺失角色，也不删除限制来获得通过。
地图显示发布与路由计算认证仍分开，不能将这六处的转向限制标为已验证生效。

## 两端在内、道路越界再返回的实际验证

2026-09-10追加，复用相同源、边界和302个图文件，没有重新下载或构图。
从原始道路中检索到兰西—民意道路 `w678816643`：

- 起点 `(124.86061250530801, 46.07147760051415)`。
- 终点 `(125.10925235870758, 46.18430857960625)`。
- 两个输入点以及引擎返回路径的两个端点都严格位于两市范围内。
- 严格吸附检查均为该道路，偏移0米；真实道路距离 **23.02公里**。
- `edge_walk`返回4段同源道路边；解码实际路径与边界相交，确实离开再进入两市范围。
- 路径在边界外的几何长度约0.171591度，仅用于证明交集非微小数值误差，不能当作公里数或道路距离。

已将该样例加入 `scripts/verify-public-road-constraints.py`，五组样例完整执行退出0，
报告在 `output/validation/public-road-constraints/five-fixtures.json`，独立复核确认边界与路径断言。
初次使用解码器的顶层导入失败，按固定安装包改用其原生扩展导出后复跑通过。
此追加关闭该固定跨界绕行样例的缺口，不覆盖所有外围道路，也不改变 `network_certified=false`。

## 异常限制的机器可读清单

审计器新增完整 `restriction_quarantine` 输出，不再仅依赖前20条问题示例。
它保留异常关系的已有成员、缺失角色和未验证项，并保守列出已有成员节点、成员道路及与这些节点
相接的全部道路；缺失from/to不得按道路形状猜测。无法定位的异常单列，不能用空排除清单声称安全。

同一实际PBF审计得到：6条异常关系、15个保守影响节点、17条保守影响道路，无法定位关系0条。
结果在 `output/validation/public-road-constraints/restriction-quarantine.json`，绑定原始源SHA256。
原始文件未改写，结构门槛仍failed，CLI仍以1退出；`applied_to_graph=false`、
`routing_release_blocked=true`明确表示清单尚未作为引擎硬排除条件落实。
这不是路网隔离已经完成，也不把正常显示地图误当成可用于道路研判。

新增保守关联道路、缺失定位和正常限制三类测试，审计专项24项通过。
