# v4.1 语义画像真实页面验证

2026-09-11（UTC+8），使用本机隔离SQLite、合成案件及测试账号，运行完整鉴权API和前端。
后端环境清理后只连接临时库，Agent关闭，内存任务代理，不读取现有业务库或模型配置。
地图和真实道路不属于本次场景，未安装地图包，不将缺少地图候选视为已有道路研判成果。

复现入口：`AIC_DISPOSABLE_SHOWCASE=1 AIC_SEMANTIC_FIXTURE=1`运行
`scripts/serve-isolated-showcase.py`，前端13043代理到18050，
再运行`scripts/verify-semantic-profile-ui.cjs`（使用已有Playwright依赖）。
命令行浏览器工具下载失败后，先用内置浏览器观察DOM和交互，再复用项目已有浏览器依赖执行脚本。

## 通过范围

- 未登录画像接口401；隔离账号登录200；正式画像接口200，无API响应模拟。
- 合成案件通过CaseService、CasePipelineService生成真实持久化画像。
- 案件详情显示否定、明确陈述、待核来源去向；展开原文、时间区间和结构化路径成功。
- 1440宽桌面面板418px，420宽窄屏面板382px，均无面板横向溢出。
- 原文及车辆JSON在读取/交互前后不变；浏览器运行异常0，外部请求0。
- 前端44文件210项回归通过，类型检查及生产构建通过，保留既有大包警告。

## 失败与修复

首次窄屏检查失败：固定三列使语义面板宽695px，超出420px窗口。
将案件页窄屏改为单列、详情优先，工具栏换行，表格保留自身滚动，并约束父级网格隐式列宽。
未隐藏筛选功能、未缩小正文。修复后同一浏览器脚本通过。
初始合成时间使用无时区值，后改为明确UTC14时，页面按UTC+8显示22时，与合成原文一致。
这属于测试夹具修正，不宣称修复了生产时区逻辑。

证据：`output/playwright/semantic-profile/report.json`、`failure.json`及截图。
`viewport-1440.png`和`viewport-420.png`为正常视口截图，已检查窄屏文字和引用可读；
长元素截图会带入固定导航，不将截图拼接位置误认成实际布局。

这不是目标服务器、真实业务准确率、所有权限变更或后台队列重启验收。
服务端脚本预先运行了画像任务；尚未证明本版本的保存→实际Redis Worker→页面全过程。
统一导出、内部道路治理及完整语义能力仍按v4.1计划继续实现。
