# 两市外围补齐后的离线矢量候选

2026-09-10；开发候选，未登记发布，不更改现行地图。

## 输入及构建

黑龙江、内蒙古、吉林同一复制时间的原始公共对象合并源：
`merged-three-regions-v1/merged.osm.pbf`，103,321,966字节，
SHA-256 `7571b3f8838f55816c1565fdc84138f7a08d582220f830e6acdf4e0b338f5f8b`。
构建后重新计算源摘要一致。

使用 `scripts/map-build/two-city-vector.json`，沿用固定tilemaker镜像摘要及镜像内标签转换脚本。
容器断网、只读根目录、丢弃全部能力、不允许提权，2核4GB，公共输入与配置只读挂载。
仅新候选目录可写；没有挂载案件、井场、技防或业务数据库。

原生缩放级别6—16，范围为两市加30公里缓冲的包围盒。显示瓦片允许简化，路网源单独保留。
没有使用公网标准瓦片抓取，没有给来源缺失对象补画几何。

## 实物输出

文件位于忽略目录 `backups/map-foundation/v4-source/20260908/vector-candidate-v2/two-city.mbtiles`。

- 实际构建退出码0。
- 文件211,681,280字节。
- SHA-256 `57ac644ba0ce3a57f88339839f7a737bfb797ae324a4f4afdeadf2a5c34e4b4d`。
- 与该候选同目录的新版地名索引有21,666地点，来源摘要绑定同一合并源。
- 旧单省矢量候选未覆盖、未删除。

## 全量内容验证

`scripts/verify-vector-map.py` 实际完整解码通过，不是抽样或只检查SQLite元数据：

- 1,128,893张瓦片，627,261张非空，16个有效显示图层。
- 跨缩放级别要素累计2,421,175；该数字不是独立现实对象数量。
- z6—16逐级瓦片数：4、9、25、72、270、1,015、3,981、15,198、52,855、211,420、844,044。
- 验证状态 `vector_content_validated`；`publish_ready`、`geographic_coverage_verified`、`road_topology_verified`仍为false。
- 相关构建脚本110项测试通过，2.61秒；后端799项通过、1项跳过；前端生产构建通过。

## 发布边界

尚需包集样式/字体/许可联合验收、真实后端登记发布和断公网冷启动。
文件存在、制图成功不代表系统已经使用本候选，也不证明道路限制可计算。
路网限制结构缺项详见 [Valhalla验证](v4.0-valhalla-build.md)。
