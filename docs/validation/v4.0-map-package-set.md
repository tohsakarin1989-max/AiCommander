# v4.0 地图包集传输完整性工作包

2026-09-10，开发验证，未发布 Stable。

实现清单见 [schema 2 契约](../superpowers/specs/2026-09-10-map-package-set-schema2.md)。新增严格清单解析、逐分片及整资产摘要核验、缺片/坏片报告、续传重检和只读命令入口。七类资源分别保留许可和署名。

验证：25 项专项通过，覆盖缺片、篡改、重排、完整文件摘要、路径和符号链接、重复键、未知字段、资源缺失、大小、续传、命令退出码、FIFO 清单和超大坐标。

增加最后三项异常清单测试之前，后端全量 605 passed、1 skipped，23.08 秒；随后修正使用 25 项专项复验。跳过的可选真实 PostGIS 环境不计作本轮已验证。本轮未改前端，未重新进行浏览器或离线地图验收。`git diff --check` 通过。

审查发现 FIFO 清单可能阻塞和超大整数坐标可能抛出异常，两项均先复现，再修复并通过全部专项测试。审查未发现其他高优先级阻断。修复前 22 项覆盖测量为模块 91%；静态工具不可用，不计作已通过。

边界：这里验证的是传输字节一致，不是 MVT 语义、字形覆盖、样式依赖闭包或道路完整性。报告始终 `publish_ready=false`。没有修改核心数据库、生产 current 地图、schema 1 导入或版本号；真实候选还不能按本契约宣称完成正式发布。

下一步：不可变资产重组、地图内容深度校验、本地字体/样式资源、正式 schema 2 导入登记及 MapLibre 页面集成。上传 API、断点续传写入、后台任务、原子发布与回滚仍待完成，不将只读续传检查冒充已交付上传功能。

## 后续工作包：受控资产重组

已增加独占暂存目录中的资产重组。冻结清单后，对每个分片重新校验，写入同一份已校验字节；核对整文件摘要，落盘并设置只读权限。全部成功才写完成标记。普通异常或可捕获中断只清理本次新目录，旧地图与输入不变。

- 重组 9 项测试、包集 25 项测试合计 34 项通过；重组模块覆盖率 98%。
- 后端全量 617 passed、1 skipped，22.91 秒；本轮未启用可选真实 PostGIS 环境。
- Python 复核通过，无高优先级问题；`git diff --check` 通过。
- 强制杀进程或主机掉电仍可能留下孤立暂存目录，不能仅凭目录存在判为完成；尚未进行主机断电恢复演练。

当前结果仍为 `assembled_integrity_verified`，明确 `publish_ready=false`。下一步转入地图内容校验和本地显示资源，不将字节重组通过当成两市地图已发布。没有更新正式数据库、地图 current、版本号或 GitHub Release。
