# 同时间点三省原始地图合并

2026-09-10；v4.0 开发工作包，非 Stable 发布声明。

## 合并实现与实物

`scripts/merge-public-osm-sources.py` 使用已固定的 pyosmium 4.3.1 流式对齐对象，
按类型及编号去重，仅合并复制时间戳相同的快照。保留节点原坐标、道路节点顺序、
原标签、关系成员及角色和对象版本。重复对象的版本、几何、标签或成员冲突一律失败，
不自动取最新值，不修补、忽略或凭空生成引用。

源文件前后摘要核对；临时候选只在成功后转为最终文件，最后写完成清单。
拒绝覆盖既有目录；消费者须读取完整清单并核对产物摘要，不能只凭文件存在判断完成。
该工具不联网、不访问业务库、不裁剪、不发布地图。

实际输入：已核对下载摘要的黑龙江、内蒙古、吉林三个 `260908.osm.pbf`，
文件头复制时间均为 `2026-09-08T20:21:01Z`。
产物目录：`backups/map-foundation/v4-source/20260908/merged-three-regions-v1/`。

- 合并产物 `merged.osm.pbf`：103,321,966 字节。
- SHA-256：`7571b3f8838f55816c1565fdc84138f7a08d582220f830e6acdf4e0b338f5f8b`。
- 保留节点15,391,281，线要素1,171,421，关系11,902。
- 去除一致重复副本465,539；未发现重复对象冲突。
- 线要素总数不是机动车道路数量，更不是可通行道路数量。

## 同源两市范围与地名索引

`two-city-region-v3/` 从合并源重新严格组装两市边界，边界内容摘要与前版一致。
30公里外围提取几何不变；因 JSON 中 margin 数值序列化为30.0，文件摘要变为
`2093e9a5284ea0de0389be32b929bd42630d341e03fbdb3b1738f1ae1bb29d49`。
对应 `provider-coverage.json` 重跑通过，不复用旧区域摘要报告。

`vector-candidate-v2/places.sqlite` 使用新的源及区域摘要重新生成：

- 21,666地点（原单省候选18,561），新增3,105。
- 31,363主名称及别名，426,004搜索片段关联，全部重建比对一致。
- 9,187,328字节，SHA-256 `a3ccdf144f53be542480e1533c9df946b898786d9422e4eb75dfafb645242839`。
- 主名称需要1,988字符，现有固定中文离线字形满足可绘制检查，缺失0。
- 本机索引及字形联合检查1.222秒；非服务器或浏览器冷启动性能证明。
- 源扫描有4项无法组装的关系几何，记录在索引元数据中，未宣称全部来源几何完整。

## 测试与待验收

新增16项测试通过，覆盖原拓扑保留、坐标/单行/版本/限制角色冲突、重复或乱序对象、
时间点不一致、空文件、删除对象、源变化、既有目录保护、CLI及调换输入顺序后的语义一致。
新增脚本测试覆盖率95%；该百分比不代表仓库整体覆盖率。
独立复核另用6万节点验证流式缓冲区生命周期，去除2万重复副本后编号和标签一致。
地图构建脚本全集99项、链条回归4项通过；前端类型检查和生产构建通过，保留既有大包警告。
后端全量回归787项通过、1项跳过（32.90秒）；跳过项不能计作PostgreSQL实物验证。

## 合并源全量引用扫描结果

`merged-three-regions-v1/reference-audit.json` 已完成；对象计数与合并清单一致。
509,243条带highway标签的线要素、342个限制关系；全部线要素缺失节点0。
限制成员仍缺3项，均与原黑龙江源审计相同：

- r15340007、r15340008缺to成员w1056196483。
- r15978440缺to成员w1177260181。

因此省级合并源的引用门禁仍为failed，不因下载邻省或完成合并而改为通过。
该审计只检查直接引用；city_boundaries中的nested_members_unverified不代替独立严格边界组装结果。
下一步须按两市目标范围提取并保留关联限制及完整成员，再重新验证实际范围内引用与构图；
不得简单删除缺失限制、把本次省级失败标为成功。

尚需两市目标提取及构图、矢量显示包重建、正式包登记与发布、断公网冷启动。
新地名索引尚未登记为正式地图资源。现有 current、业务库和版本号未改变。
