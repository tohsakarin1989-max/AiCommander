# 核心后端候选镜像核对

2026-09-10，使用正式 `backend/Dockerfile` 构建，不以地图Worker镜像替代。

## 已定位并修复的阻碍

1. 首次构建停在固定基础镜像的元数据阶段。只读进程检查确认
   `docker-credential-desktop get`卡住；公共registry接口正常响应认证挑战。
   本次构建进程被显式终止，退出130。未把观察超时当成构建结束。
2. 使用一次性空auths配置及现有本机Docker socket访问公开固定摘要镜像，
   未修改用户Docker配置或登录。随后成功取得要求的Python基础镜像，开始真实依赖安装。
3. 构建上下文检查发现旧排除规则只排除data/chroma_db，仍包含本机data/map-packages。
   将backend/.dockerignore改为排除整个data目录；运行地图仍由独立命名卷挂载。
   新回归先1失败2通过，修复后部署配置、健康及镜像边界28项通过。

## 首轮限制（已由后续重建解决）

当前在运行的依赖构建已在修改排除规则之前读取上下文，因此即使该次构建成功，
也不能当成修复后的干净交付镜像，不得推送或导出它。
需在它终止后按新排除规则重新构建（可复用已完成的依赖层），再实际检查：

- 镜像无运行地图、业务数据库、宿主环境配置；地图初始目录为空。
- 非root应用导入、当前迁移和受控服务调用可用。
- 记录最终镜像ID与构建结果，再更新候选交付状态。

此次构建只使用本机Docker daemon，未上传GitHub或镜像仓库。
一切运行数据保留宿主机原文件，不因修复排除规则而删除。

## 干净镜像重建及实际运行通过

首轮完成后按新排除规则重新构建，依赖层命中缓存，构建上下文由168.6MB降至6.972MB。
正式Dockerfile未改为地图Worker镜像，也未借宿主site-packages替代容器依赖。

最终镜像：`sha256:80726580e77fbf7632ee7f8ac35eb22914254587ef2d411ed04f0bc1e2a44f4b`，
本机标签`aicommander-backend:v4-core-candidate`；构建退出0。

实际运行 `scripts/verify-core-image.py`：无宿主挂载，network=none，read-only根目录，
仅/tmp提供256MB临时盘，cap-drop ALL，no-new-privileges，UID10001。

- 检查/app内无数据库、MBTiles或环境配置文件，data与地图初始目录无文件。
- 临时合成SQLite从空库升级到f830b2152595，全部迁移实际执行。
- /health/live返回200，未登录案件读取401。
- 实际鉴权登录、保存一条合成案件、读取该案件均200。
- Agent和向量能力关闭、Redis不可用，案件主链路仍通过；退出0，临时容器自动回收。

不代表PostgreSQL全栈部署、真实公网服务器或目标机性能；这些边界与既有专项记录分开。
首轮旧镜像4aa11597298e不用于交付，按精确ID清理；宿主地图文件没有删除。
