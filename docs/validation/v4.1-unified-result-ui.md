# v4.1 统一成果案件详情验证

## 本次交付

案件详情的语义画像和双域候选改为从同一个`/api/cases/{id}/results/latest`读取，不再分别获取两套内容。
复用现有深色界面、系统字体和窄屏布局；统一面板包含原始记录摘要、关联条件、关键缺项、候选、全部支持
与反向证据/缺口、引用及版本。更新未完成时明确标记上次结果，不增加“运行智能体”的必需操作。

成果接口报错时隐藏缓存原文和地图，404与网络故障分别说明；切换案件时不显示前案缓存。
地图输入只来自冻结成果中的案件坐标、候选区域与地图版本，不使用最新案件坐标替换旧证据。
新成果分数显示“规则支持度”，旧版地图指标标注未经概率校准；不再用“置信度百分比”冒充概率。
未携带时区的存储时间明确加注，不默认当作本地时间与原文时刻比较。

## 验证证据

2026-09-11，本机隔离数据库、合成案件、完整鉴权API及真实浏览器，无成功响应模拟。
沿用`scripts/serve-isolated-showcase.py`及增强后的`scripts/verify-semantic-profile-ui.cjs`：

- 登录后读取真实自动生成的成果，成果画像编号与源画像一致。
- 页面实际请求统一成果，不再请求旧的独立画像/融合接口。
- 1440px和420px场景通过，原文、时间表达、结构化资料可展开，面板无横向溢出。
- 阅读及展开前后，原始案情和车辆数据不变。
- 真实内容加载后仅中断成果接口传输，缓存原文隐藏；恢复网络后重新显示。
- 浏览器运行异常0，外部请求0；故障注入单独标识，不冒充真实网络故障。

输出位于`output/playwright/semantic-profile/report.json`及`unified-1440.png`、`unified-420.png`。
已实际检查桌面及窄屏截图。截图中发现无时区存储时间与原文时间容易混淆，补充说明并复验。
前端全量45文件218项通过；追加时区说明后组件9项及类型检查通过。生产构建通过，保留既有大包警告。
后端链条、成果接口、自动成果服务链相关17项通过（1.69秒）。

## 未覆盖范围

本场景没有地图包及空间候选，不能证明真实地图候选展开、道路计算或实际瓦片离线验收。
冻结地图模型输入、分数标注和候选证据列表由前端专项覆盖，实际含地图数据的页面仍需后续集成验证。
本次服务端预先执行画像任务，没有启动真实Redis Worker；不是现场服务器或后台故障演练。
分析工作台、Word/PDF导出继续待办，不把案件详情接入等同于整个v4.1完成。

## 报告中心增量验证（2026-09-11）

报告中心新增案件成果目录，保留原会议报告。通过`GET /api/case-results`服务端检索与分页，
列表仅包含当前授权案件；每份成果的引用再次校验，不可用成果不返回版本及证据内容。
案件详情链接携带固定成果编号，报告中心复用同一成果面板和冻结地图输入，不重新调用模型。

隔离浏览器验证记录`reportCenterSameSnapshot=true`与`catalogSearch=true`：从真实详情链接进入，
读取固定编号的内容与详情成果逐项相等；未命中检索和命中检索均验证。1440px与420px无横向溢出，
浏览器异常和外部请求均为0。证据为上述report.json及report-center-1440.png、report-center-420.png。
实际检查420px截图，修复查看按钮折行和生成时间格式；本表由UTC写入的生成时间恢复明确时区，
页面按北京时间显示。历史案件未注明时区的案发时间仍保留原值，不作推测性转换。

本次复跑成果接口、权限、自动生成及补齐36项通过（2.52秒）；前端成果面板及时间展示11项通过，
TypeScript检查通过。未将之前的全量构建当成本次最终全量发布验收。
本场景依然使用合成数据，没有地图包、真实队列或现场部署；Word/PDF导出尚未提供。

## 案件研判工作台接入（2026-09-11）

`LatestCaseResult`在工作台按案件编号读取同一latest接口，使用同一面板、地图输入与固定版本链接。
默认不调用旧版`case-intelligence/workbench`及`llm-context`；旧工具通过明确的兼容入口展开，
标注即时计算内容不是冻结成果，时间窗与条数不影响统一成果。全局模式不再被默认选案逻辑覆盖。
本次没有将日常任务工作台改成另一个分析系统。

真实浏览器合成场景通过：`workbenchSameResult=true`，校验latest请求、固定版本链接及旧接口未调用。
工作台1440px/420px成果面板无横向溢出，截图已检查；随后返回案件详情做故障隐藏/恢复，继续通过
报告中心检索及固定内容一致性检查。运行异常与外部请求为0，临时服务正常退出。
前端相关14项通过，类型检查通过，生产构建通过（保留既有大包警告；随后轮询条件修改补做类型检查）。

失败记录：首次尝试在旧案件详情等待语义标题30秒超时，未确定根因，后续两次均加载成功；第二次
脚本进入工作台后未返回详情就执行原详情故障步骤，已修正流程，第三次完整通过。不将单次最终通过
当作持续稳定性或现场性能证明。旧版工具展开后全部交互、全局模式完整业务回归仍需后续验证。
Word/PDF及含真实地图包的导出仍待实施。
