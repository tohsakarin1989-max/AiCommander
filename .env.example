# Copy to .env for Docker Compose or local development.
# Generate values locally; do not commit real secrets.

DB_PASSWORD=<generate-a-local-database-password>
SECRET_KEY=<generate-a-local-secret-key>

# Optional model provider keys.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
